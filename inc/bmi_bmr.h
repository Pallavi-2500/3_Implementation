#ifndef __bmi_bmr_H__
    
    #define __bmi_bmr_H__
    
    double calcBMI();
    const char* bmiResult(double bmi);
    double bmrResult(int gender, double weight, double height, double age);
    double calcBMR();

#endif